# the stuck family's closed twin (HVM4 red_id, red_simple): the identity
# ref applied to a literal, on Nat (HVM4's 42 was a native number).
import Base

law id:
  for a: Nat
  Nat

def id(a):
  a

law main:
  Nat

def main():
  id(42n)

#|42n
