# The proofs of the echo server laws: each case of echo_step computes.
import Base
import ./main.bend as Echo
import ./LAWS.bend as Laws

def Laws.echoes_data(c, t):
  {==}

def Laws.closes_on_end(e):
  ({==}, {==})
